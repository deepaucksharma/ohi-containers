name: Integration Tests (Disabled - Use Complete Validation)

on:
  workflow_dispatch:  # Only manual triggers, no automatic triggers

jobs:
  integration-tests-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notification
        run: |
          echo "This workflow is disabled to avoid duplication."
          echo "Please use the Complete Validation workflow instead."
          exit 0  # Always succeed
